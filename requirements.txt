# Core dependencies
opencv-python-headless>=4.5.0
numpy>=1.21.0
matplotlib>=3.5.0
tqdm>=4.60.0

# Thermal camera interface
# Pinned exactly: this is the version wrapper_boson.py's telemetry parsing
# and camera detection have been tested against.
flirpy==0.6.2

# Data compression
zstandard>=0.15.0

# Serial communication for telemetry/FFC control
pyserial>=3.5

# Video4Linux device metadata (Linux only; used for camera discovery)
pyudev>=0.21.0; sys_platform == "linux"
